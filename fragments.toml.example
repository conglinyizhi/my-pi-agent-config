# fragments.toml 的模板：真身（fragments.toml）本机私有、不入库，换机器时从这里复制：
#
#   cp fragments.toml.example fragments.toml
#
# 输入框里的 &碎片 —— 打 &名字 按回车，换成下面这些正文。
# 改完不用 /reload，下次输入就生效（按文件 mtime 重读）。
#
# 形状（照抄改就行）：
#
# [[fragment]]
# name = "单步计划"
# desc = "仅调查不行动"
# text = """
# 对于这一步，只做调查、不要动手。先给证据与结论，等我点头再动。
# """
#
# 一条正文可以挂多个触发词，写在它自己的 aliases 里（别名之间、以及跟别人的名字
# 都不能重，重了跳过并提示）：
#
# [[fragment]]
# name = "关于我"
# aliases = ["core-prompt", "我"]
# desc = "背景资料入口"
# text = "读 ~/disk/core-prompt/ 下面相关内容，再继续这个问题"
#
# 名字允许字母、数字、下划线、连字符与冒号（含中日韩等广义字母），不能有空白。
#
# 命令：
#   /frag:list            列出全部碎片，选中即插进输入框
#   /frag:build <名字>    把某条正文插进输入框，改完再发
#   /frag:add             交互式加一条
#
# 带参数的调用 `&名字(参数)` 会交给注册的 provider 展开，参数里可以放东西，
# 例如 &img(3) 引用照片池里的第 3 张（把那张图附到这条消息上）。
