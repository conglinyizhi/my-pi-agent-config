{
  "type": "module",
  "imports": {
    "#lib/*": "./lib/*.mjs"
  },
  "scripts": {
    "build:gui-editor": "cd extensions/editor-gui/gui && esbuild app.ts --bundle --platform=node --format=esm --external:electron --outfile=app.js && rsbuild build",
    "build:gui-gate": "cd extensions/permission-gate/gui && esbuild app.ts --bundle --platform=node --format=esm --external:electron --outfile=app.js && rsbuild build",
    "build:gui-setup": "cd extensions/trident-queue/gui && esbuild app.ts --bundle --platform=node --format=esm --external:electron --outfile=app.js && rsbuild build"
  },
  "dependencies": {
    "@rsbuild/core": "^2.1.8",
    "@rsbuild/plugin-vue": "^2.0.1",
    "handlebars": "^4.7.8",
    "smol-toml": "^1.7.0",
    "vue": "^3.5.40"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "^0.80.10",
    "@earendil-works/pi-ai": "^0.80.10",
    "@earendil-works/pi-coding-agent": "^0.80.10",
    "@earendil-works/pi-tui": "^0.80.10",
    "@types/node": "^25.9.3",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.0",
    "typebox": "^1.2.8",
    "typescript": "^6.0.3"
  }
}
