auth.json
providers.toml
settings.json
sessions/
npm/
git/
node_modules/
.DS_Store
mcp-cache.json
docs/superpowers/
.worktrees/
*.d.ts
skill-repo/*
!skill-repo/repo.toml
skills/*
!skills/clyzhi/
# 外部技能正式发现入口由 skill-boot 动态维护，入口和 clone 内容均为运行时产物
skills/external/
# skill-vault 顶层符号链接：skill-boot 迁移前兼容入口，非 git 管理
skill-vault/*
!skill-vault/clyzhi/
skill-states.json
skillful-discovery-state.json
# skillful 显隐设置独立 toml，避免写 settings.json/tracked.json 引起仓库变动
skillful-settings.toml
.pi-sync-state.json
*.log
models-store.json

# 三叉戟
providers.roles.toml
queue/
permission-gate-reasons.csv

# GUI 产物
**/gui/dist/
# TS→JS 编译产物
**/gui/app.js
**/gui/app.mjs
**/gui/app.js.map
**/gui-review/app.js
**/gui-review/app.mjs
**/gui-manager/app.js
**/gui-manager/app.mjs
permission-gate-reasons.json
extensions/sandbox-permissions/sandbox-paths.json

# git worktrees
.worktrees/

# SDD workspace
.superpowers/
# 独立工程（不属于 pi 配置，剥离）
dsh-notify/
minicpm5-deploy/
.go-cache/
.go-cache/
scripts/vendor/landlock-run
scripts/vendor/network-block-run

# 审核模型池（供应商配置/API key 不入库）
extensions/sandbox-permissions/review-pool.toml

# OpenCode Zen 免费模型探活快照（运行时状态，不入库）
extensions/opencode-free/models.json

# wails-gui 根目录构建产物
wails-gui/wails-gui

# message-page 本机偏好（上次选择的模型，不入库）
message-page-model.toml

# 模型选择偏好（本机运行时状态，不入库）
model-selection.toml

# 本机临时备忘（subagent 审批链路排查笔记，不入库）
warn.txt
